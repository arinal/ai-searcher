apply plugin: 'java'

version = 0.0

task wrapper(type: Wrapper) {
    gradleVersion = '2.2.1'
}

task run(type: JavaExec) {
    main = "org.stei.ui.konsole.Program"
    classpath = files("build/libs/${project.name}-${version}.jar")
}

run.dependsOn(build)